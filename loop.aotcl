class Loop {
  function Main() {
    while (true) {
      i = 2 + 4;
      Game.Print(i);
      return;
    }
    for (i in Obj.List) {
      i = 2 + 4;
      Game.Print(i);
      return 0;
    }
    for (i in Range(0, 10, 2)) {
      Game.Print(i);
      return Game.Print(i);
    }
    Range(a);
    return;
    return 4;
    wait 1.0;
    wait Range(1, 20, 4);
  }
}
