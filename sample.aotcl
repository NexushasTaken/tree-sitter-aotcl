class Main {
  function OnGameStart() {
    self.field("" + Convert.ToString(a) + "" + Convert.ToString(b));
    #                       ^ function.method.call
    #                                                  ^ function.method.call
  }
}
