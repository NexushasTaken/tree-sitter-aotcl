# @variable, @string, @boolean, @type, @function?, @function.method, @constructor, @keyword
class CustomClass {}
#                 ^ punctuation.bracket
#                  ^ punctuation.bracket
# <- keyword.type
#     ^ type
class Main {
# <- keyword.type
#     ^ type.builtin
  Instance = 256;
  # <- variable.member
  _private = 3.14;
  # <- variable.member

  str = "Hello, World";
  #     ^ string

  bool_true = true;
  #           ^ boolean
  bool_false = false;
  #            ^ boolean

  function OnGameStart(param1, param2) {
  # <- keyword.function
    #                  ^ variable.parameter
    #                          ^ variable.parameter
    #                        ^ punctuation.delimiter
    wait 0.1;
    # <- keyword.coroutine
    self.Instance = 1024;
    # <- variable.builtin
    self.field.hello();
    #               ^ punctuation.bracket
    #                ^ punctuation.bracket
    #   ^ punctuation.delimiter
    #    ^ variable.member
    #          ^ @function.method.call
    op = -1 + 2 - 3 * 4 / 5;
    #  ^ operator
    #    ^ operator
    #       ^ operator
    #           ^ operator
    #               ^ operator
    #                   ^ operator
    #                      ^ punctuation.delimiter
    op += 1;
    #  ^ operator
    op -= 1;
    #  ^ operator
    op *= 1;
    #  ^ operator
    op /= 1;
    #  ^ operator
    condition = true || false && true;
    #                ^ operator
    #                         ^ operator
    equality = true == false != true;
    #               ^ operator
    #                        ^ operator
    comparison = 1 < 2 > 3 <= 4 >= 5;
    #              ^ operator
    #                  ^ operator
    #                      ^ operator
    #                           ^ operator

    negate = !true;
    #        ^ operator
    self.field = Main.call() + 4;
    #            ^ type.builtin
    #                 ^ @function.method.call
    #                     ^ punctuation.bracket
    #                      ^ punctuation.bracket
    op = -1 + 2 - 3 * 4 / 5;
    self.field = CustomClass.call();
    #            ^ variable
    #                        ^ @function.method.call
    self.field = CustomClass.a.b.call();
  }
  function Loops() {
    for (i in Range(1, 2, 3)) {
    # <- keyword.repeat
    }
    while (true) {
    # <- keyword.repeat
    }
    return true;
    # <- keyword.return
    return;
    # <- keyword.return
  }
  coroutine Conditional() {
  # <- keyword.coroutine
    if (true) {
    # <- keyword.conditional
    } elif (false) {
      # <- keyword.conditional
    } else {
      # <- keyword.conditional
    }
  }
}
